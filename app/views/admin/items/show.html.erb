<div clas="row">
  <div class="content-box-large">
    <h2><%= @item.name %></h2>
    <div class="item-price">
      <p>NT<%= number_to_currency(@item.price) %></p>
    </div>
    <div class="default-item-photo">
      <%= item_photo(@item.default_photo) if @item.default_photo %>
    </div>
  </div>
</div>