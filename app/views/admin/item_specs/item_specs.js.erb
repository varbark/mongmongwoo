<% if @item_spec.valid? %>
  $("#new-item-spec").remove();
  // $("#specs-table-list").append("#spec-<%= @item_spec.id %>");
  $("<%= j (render @item_spec) %>").appendTo("#specs-table-list");
<% end %>